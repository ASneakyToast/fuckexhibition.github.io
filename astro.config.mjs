import { defineConfig } from 'astro/config';

export default defineConfig({
  site: "https://fuck-exhibition.com",

  /* Only use when deploying other ways */
  // base: "/fuckexhibition.github.io",
  // outDir: "../dist",
});
